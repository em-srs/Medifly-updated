server.port=${PORT:5000}
spring.application.name=medifly-backend

# PostgreSQL Datasource Configuration (Render-managed)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://dpg-d9j4hanlk1mc73fj7lv0-a.singapore-postgres.render.com:5432/medifly_db?sslmode=require}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:medifly_db_user}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:EfOClpGGcToocXd7zYlUrezd6mb9F7gN}

# JPA / Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Connection Pool Settings (prevent connection timeout on Render)
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.maximum-pool-size=5

# JWT Security
jwt.secret=${JWT_SECRET:supersecretjwtkey_for_medifly_must_be_at_least_256_bits_long_for_hs256_algorithm}
jwt.expiration.ms=604800000

# Logging
logging.level.org.springframework.security=INFO
logging.level.org.hibernate.SQL=DEBUG
logging.level.com.medifly=DEBUG
